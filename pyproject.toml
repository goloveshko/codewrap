[project]
name = "codewrap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sergey", email = "golsa.job@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pathspec>=1.1.0",
    "rich>=15.0.0",
    "ruff>=0.15.12",
    "tiktoken>=0.12.0",
    "typer>=0.24.2",
]

[project.scripts]
codewrap = "codewrap.main:app"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
